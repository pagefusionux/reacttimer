var React = require('react');

const Countdown = React.createClass({
  render: function() {
    return (
      <div>
        Countdown component.
      </div>
    );
  }
});

module.exports = Countdown;

var React = require('react');

const Controls = React.createClass({
  render: function() {
    return (
      <div>
        Controls component.
      </div>
    );
  }
});

module.exports = Controls;
